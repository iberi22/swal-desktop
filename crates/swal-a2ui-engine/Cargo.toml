[package]
name = "swal-a2ui-engine"
version = "0.1.0"
edition = "2021"
authors = ["SouthWest AI Labs <agent@southwest-ai-labs.com>"]
description = "Declarative JSON to Native Component Tree parser for SWAL Generative UI"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
