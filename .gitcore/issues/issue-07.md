# SWAL-07: Dashboard Dynamic Palette Reactivity & Theme Indicator

## Scope
Ensure `eww.scss` compiles dynamically with CSS variables and that `eww reload` updates all colors without visual artifacts.
